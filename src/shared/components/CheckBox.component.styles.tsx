import {StyleSheet} from 'react-native';

export const CheckBoxComponentStyles = StyleSheet.create({
  textStyles: {fontSize: 18},
  containerStyle: {width: '100%', marginLeft: -1, paddingHorizontal: 0},
});
